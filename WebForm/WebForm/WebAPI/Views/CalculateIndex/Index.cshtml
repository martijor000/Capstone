@model WebAPI.Models.Items

    @for (int i = 0; i < Model.GroceryList.Count; i++)
    {
        @Html.TextBoxFor(model => model.GroceryList[i])
    }

    <button type="submit">Subscribe</button>
}

